/**
 * API composable placeholder.
 *
 * Composables coordinate UI-facing behavior and keep view components thin.
 * They orchestrate services, not direct transport implementations.
 */
export const useApi = () => {
  return {};
};

