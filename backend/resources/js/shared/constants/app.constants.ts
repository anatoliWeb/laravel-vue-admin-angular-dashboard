/**
 * Shared constants placeholder.
 *
 * Constants that are reused across modules should live here
 * to avoid duplication and hidden cross-module dependencies.
 */
export const APP_NAME = 'Vue Admin';

