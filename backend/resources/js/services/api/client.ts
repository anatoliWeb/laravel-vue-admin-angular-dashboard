/**
 * API client placeholder.
 *
 * Future responsibility:
 * - central HTTP instance
 * - request/response interceptors
 * - transport-level concerns (headers, retries, base URL)
 *
 * Keeping transport logic in services prevents view/components from
 * depending directly on networking details.
 */
export const apiClientPlaceholder = {};

